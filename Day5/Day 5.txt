Day 5 DSA
-----------
-> finding maximum from two numbers : Math.max(a,b)
 if there are 3 numbers we can find max = Math.max(c,Math.max(a,b))
-> input char: sc.next().trim().charAt(0) // trim will trim the blank spaceces from starting and ending 

-> can use Math.PI instead of 3.14
-> round of the double value we can use: System.out.printf("Area of circle : %.2f", area);    // printf with ,

in java printf - for formatted output 
the , separates formatted string(%.2f) (not concatenation(+))

%.2f in printf means:

% = format specifier
.2 = show 2 digits after the decimal
f = floating-point number

The triangle inequality says:
For any triangle, the sum of any two sides must be greater than the third side.


isosceles Triangle(two sides are equal)
--------------------------------------
Area = 1/2* b*h 
----
here b= b/2
a^2 = h^2 + b^2(Pythagoras theorem)
h = root(a^2 - (b/2)^2) = root(a^2 - b^2/4) = root((4a^2 -b^2)/4) = root(4a^2- b^2)/2

area = b/2* root(4a^2 - b^2)/2 = b/4.0*root(4a^2-b^2)

volume of cone = 1/3*PI*r*r*h
why 1/3 -> cone's volume is 1/3 of a cylinder with same base and height